import React from 'react'

export const metadata = {
    title: 'Crytos - NexaCoins',
}

export default function layout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
